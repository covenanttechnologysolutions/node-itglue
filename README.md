# node-itglue

```js

const ITGlue = require('node-itglue');

const itg = new ITGlue({
  apikey: '// your api key',
 
  // optional
  eu: false, // set true if you're connecting to the European datacenter
  timeout: 10000, // timeout in ms
   
})

```
